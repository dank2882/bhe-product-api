# Shipping Containers initial release — September 23, 2026

Accepted by Dan: FBC ownership, FBC Staff Tools access, existing-backend domain,
Dan-only initial access. His subsequent approval authorized implementation and
requested a readable shipment-history summary.

## Implemented and deployed

- Core runtime commit `73db33e`: `bhe-product-api-00293-dcn`, observed at 100%
  traffic. Previous production revision `bhe-product-api-00292-qnx` retained for
  rollback; existing zero-traffic tags preserved.
- Gateway commit `fe77a85`: `fbc-staff-tools-mcp-entra-prod-00054-7qk`, observed at
  100% traffic. Previous revision `fbc-staff-tools-mcp-entra-prod-00053-g47`
  retained for rollback; existing tags preserved.
- Administrative import script/documentation commit `8ae9e28` is separate from
  the core runtime build; it does not change runtime behavior.
- Four logical shipping scopes and explicit verified individual-owner allowlist.
  Dan's profile updated from version 5 to 6, preserving prior permissions and
  identity aliases. Existing travel list was not reused because it includes
  another staff member. Administrator/break-glass access does not imply shipping.
- Three tools advertised by live public MCP discovery; existing delegated OAuth
  transport scope preserved. Anonymous shipping call returned HTTP 401.

## Data and validation

Core checks and all 673 tests passed. Gateway checks and all 195 tests passed.
Targeted cases include permission denial, version conflict, idempotency, revised
paperwork acceptance, all-container delivery/closeout, separate currencies,
rebill/waiver accounting, private files and preview-bound imports.

Research: 2,348 search-result messages, 75 fully reviewed messages/threads and
selected attachments. This is not an exhaustive read of every matching email.
The resulting 35 groups include incomplete shipments and proposals, not 35
verified deliveries. Eight future-load proposals were staged separately.

43 records and their immutable first revisions are saved in the owning FBC
collections. Six original files were saved in the existing private bucket.
Migration used the actual Google Cloud IAM administrator identity, recorded as
such in audit; it did not impersonate Dan's end-user OAuth identity. Exact batch
preview hash, source-key deduplication, atomic per-record writes, independent
record read-back and downloaded-original SHA-256 comparison were verified.
Private receipts and original evidence remain outside Git.

Three reported actual-date interval samples were recovered: Honduras 2020 ocean
5 days, Zambia 2019 port-to-door 17 days, Baguio 2025 port-to-door 26 days.
The Baguio invoice ship field is not treated as actual vessel departure.
Uncertain dates/links and missing payments stay explicitly unresolved.

## Outstanding acceptance and limitations

- Fresh signed-in FBC Staff Tools and phone acceptance remain pending; the current
  session has a cached tool catalog. Public tool discovery and IAM migration do
  not prove end-user acceptance. Refresh the connection, then test list/schedule,
  a new plan with two containers, versioned update and authorized file retrieval.
- Developer Tools controlled registry actions were unavailable in this session.
  This release evidence is committed in Git; registry release/workstream evidence
  has not been saved and must be reconciled through its authorized operations.
- A 14,112,694-byte broker PDF exceeds the initial 10 MiB upload limit. Metadata,
  source email link, hash and itemized statement are saved; the original remains
  in the private research folder. Other user-provided scan binaries were not
  locally available; their extracted facts cite the supplied images.
- Cost scopes differ; reported payments and overlapping reimbursements are not
  settled spending. Historical document requirements require reconfirmation.

No email, carrier booking, payment, signature or staff-wide grant was performed.
